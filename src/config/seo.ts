import { siteConfig } from "./site"

export const SEO = {
  title: siteConfig.name,
  titleTemplate: `%s - ${siteConfig.name}`,
  defaultTitle: `${siteConfig.name} - Smart WiFi Marketing Solutions`,
  // ... other SEO settings
} 